Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

Application URL: https://{{ .Values.global.domain }}

Default User Credentials:
  Username: {{ .Values.secrets.defaultUserName }}
  Email:    {{ .Values.secrets.defaultUserEmail }}
  Password: {{ include "eneo.generateDefaultUserPassword" (dict "value" .Values.secrets.defaultUserPassword "context" .) }}

To retrieve the password later, run:
  kubectl get secret {{ include "eneo.fullname" . }}-secrets -n {{ .Release.Namespace }} -o jsonpath='{.data.DEFAULT_USER_PASSWORD}' | base64 -d
